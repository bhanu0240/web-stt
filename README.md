# web-stt

UI for speech to text transcription


## Features

- Transcribe recorded audio using the hosted models
- Live transcribe using browser speech recognition API

## Quickstart

- Clone the repo - `https://github.com/gitcommitshow/web-stt`
- Install dependencies `npm install`
- Start the project `npm start` (it will build the frontend assets and start the express server)